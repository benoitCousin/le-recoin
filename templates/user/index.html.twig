{% extends 'user.html.twig' %}

{% block title %}Profil{% endblock %}

{% block body %}
        
        <div class="container">
            <div class="row">
                <div> <h2>Profil de {{ app.user.FirstName }}</h2></div>
                    <div class="row row-col-3">
                        <div class="col"> Nom: {{ app.user.Name }}</div>
                        <div class="col">Prénom : {{ app.user.FirstName }} </div>
                        <div class="col"> Email : {{ app.user.Email }} </div>
                    </div>
                    <div>
                        
                    </div>
                    <div class="row">
                        <div class="row"><button class="btn btn-primary" type="submit">Modifier le profil</button></div>
                        <div class="row"><button class="btn btn-primary" type="submit">Modifier le MDP</button></div>
                    </div>
                    <div class="row">
                    
                        <h2> Vous avez {{ app.user.products|length }} annonces en ligne</h2>
                    </div>
                    
                    {% for product in app.user.products %}
                        <div class=""> Titre:</div>
                        <div class=""> {{ product.title }}</div>

                    {% endfor %} 
        </div>

{% endblock %}
